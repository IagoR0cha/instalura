import { StyleSheet } from 'react-native';

const estilo = StyleSheet.create({
    mesmaLinha: {
        flexDirection: 'row',
        alignItems: 'center',
    },

    imgSend: {
        width: 30,
        height: 30,
    },

    comentarios: {
        marginHorizontal: 12
    }
});

export default estilo;